import Link from 'next/link';
import { Database,ArrowRight,ShieldCheck } from 'lucide-react';
import { Logo } from '@/design-system';
import { ThemeToggle } from '@/components/theme-toggle';
export const metadata={title:'Conectar ambiente'};
export default function Setup(){return <main className="wr-auth min-h-dvh px-6 py-8"><header className="mx-auto flex max-w-4xl items-center justify-between"><Logo/><ThemeToggle/></header><section className="wr-section mx-auto mt-14 max-w-2xl p-7 sm:p-10"><span className="mb-6 inline-flex rounded-2xl bg-primary/10 p-4 text-primary"><Database className="size-7"/></span><p className="text-xs font-medium uppercase tracking-[.16em] text-primary">CONFIGURAÇÃO DO AMBIENTE</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Vamos conectar seus números.</h1><p className="mt-4 text-sm leading-7 text-muted-foreground">A aplicação está preparada. Falta conectar o projeto Supabase para habilitar os acessos e carregar os dados reais da empresa.</p><ol className="my-8 space-y-5 text-sm leading-6"><li><strong>1. Configure as variáveis</strong><p className="text-muted-foreground">Preencha a URL e a chave pública no arquivo <code>.env.local</code>, usando <code>.env.example</code> como referência.</p></li><li><strong>2. Aplique as migrações</strong><p className="text-muted-foreground">Execute os arquivos de <code>supabase/migrations</code> na ordem indicada no README.</p></li><li><strong>3. Habilite o acesso</strong><p className="text-muted-foreground">Configure os endereços de retorno no Supabase Auth, reinicie a aplicação e crie seu acesso.</p></li></ol><p className="mb-8 flex gap-3 rounded-xl bg-primary/10 p-4 text-sm leading-6 text-muted-foreground"><ShieldCheck className="mt-1 size-5 shrink-0 text-primary"/>O dashboard só mostra informações de uma empresa após autenticação. Nenhum saldo de exemplo é utilizado na aplicação real.</p><Link href="/login" className="inline-flex h-11 items-center justify-center gap-3 rounded-xl bg-primary px-5 text-sm font-semibold text-primary-foreground">Ir para o acesso<ArrowRight className="size-4"/></Link></section></main>;}
