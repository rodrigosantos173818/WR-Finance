import Link from 'next/link';
import { ChartNoAxesCombined,Check,ShieldCheck } from 'lucide-react';
import { Logo } from '@/design-system';
import { ThemeToggle } from '@/components/theme-toggle';
export function AuthShell({title,description,children,configured=true}:{title:string;description:string;children:React.ReactNode;configured?:boolean}) {
  return <main className="wr-auth min-h-dvh"><header className="mx-auto flex max-w-7xl items-center justify-between px-6 py-7 lg:px-10"><Link href="/login" aria-label="WR Finance, página de entrada"><Logo/></Link><ThemeToggle/></header><div className="mx-auto grid min-h-[calc(100dvh-110px)] max-w-7xl items-center gap-12 px-6 pb-16 lg:grid-cols-2 lg:px-10"><section className="hidden lg:block"><span className="mb-7 inline-flex items-center gap-2 rounded-full border border-primary/20 bg-primary/10 px-3 py-1.5 text-xs font-medium text-primary"><span className="wr-dot"/>CLAREZA PARA CRESCER</span><h1 className="max-w-lg text-5xl font-semibold leading-[1.16] tracking-tight">Seu próximo passo começa com <span className="text-primary">controle.</span></h1><p className="mt-6 max-w-md text-lg leading-8 text-muted-foreground">Controle financeiro simples, visual e inteligente para empresas.</p><div className="mt-10 space-y-4">{['Saiba quanto sua empresa tem disponível.','Antecipe o que entra e o que sai.','Tome decisões com uma visão clara.'].map(item=><p key={item} className="flex items-center gap-3 text-sm text-muted-foreground"><Check className="size-4 text-primary"/>{item}</p>)}</div><div className="mt-12 flex items-center gap-4 border-t border-border pt-7 text-xs text-muted-foreground"><ChartNoAxesCombined className="size-5 text-primary"/><span>Mais clareza nos números.<br/>Mais confiança para seguir em frente.</span></div></section><section className="mx-auto w-full max-w-[440px] rounded-3xl border border-border bg-card p-7 shadow-[var(--wr-shadow)] sm:p-10"><div className="mb-8"><h2 className="text-2xl font-semibold tracking-tight">{title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{description}</p></div>{!configured&&<div className="mb-6 rounded-xl border border-warning/20 bg-warning/10 p-4 text-sm leading-6"><p>Este ambiente ainda aguarda a conexão com o Supabase.</p><Link href="/setup" className="mt-2 inline-block font-medium text-warning hover:underline">Ver configuração necessária →</Link></div>}{children}<p className="mt-8 flex items-center justify-center gap-2 text-xs text-muted-foreground"><ShieldCheck className="size-4"/>Um espaço seguro para o seu negócio.</p></section></div></main>;
}
